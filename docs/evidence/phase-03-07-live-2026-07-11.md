# Phase 3–7 live verification evidence — 2026-07-11

## Final deterministic gate

- `.venv/bin/pytest -q`: 147 passed; one upstream Starlette deprecation warning.
- `.venv/bin/ruff check .`: passed.
- `git diff --check`: passed.
- Hermes `followthrough-operator` skill validation: passed.
- Public and local health: database, complete archive, authentication, control plane, and orchestrator healthy.

## Hermes execution and Discord

- Worker command line used profile `followthrough`, skill `followthrough-operator`, and only `kanban,skills,web` toolsets.
- Live workers used the isolated profile; no Docker/MCP child or broad credential environment was present.
- Real Discord delivery IDs: completion `1525623027277107382`; blocked `1525622226416566282`.
- Read-back found one completion and one blocked notification, no duplicate hashes, and no raw-transcript marker.
- Emergency pause parked the live task in 6.14 seconds. After correcting a profile-resume bug, least-authority resume completed in 5.236 seconds. Both satisfy the 10-second target.
- A stale-reconciliation compare-and-swap regression test now prevents superseded cards from being resurrected.

## Repository sandbox and typed actions

- Fresh PyPA `sampleproject` run:
  - commit `621e4974ca25ce531773def586ba3ed8e736b3fc`;
  - tree `c7d439931f56fa21023a7a0e615b91f5699c1827`;
  - receipt `5e980b63e973028d729dc5e6a2ab6c13d7a9658a989a011dcdc3abddb8b2a9a4`;
  - `systemd-service+bubblewrap`, network disabled, exit 0, no timeout.
- Fresh TODO card `t_8cd1e5da` completed once and emitted typed effect `43c1f426-c6d2-46cd-bc19-c6c22498e771`.
- The effect auto-created private task `task_550f90f0e060c539e074e3a6` with an idempotent reversible receipt.
- Other live adapter checks completed for Google Calendar, owner Discord DM, private task, and sandbox purchase. GitHub issue, deployment, and live purchase remain approval-gated.

## Samsung Flip and Omi

- ADB target: Samsung `SM-F776U1`, Android 17, `100.96.0.1:5555`.
- Omi package `com.friend.ios`, version 1.0.544.
- Conversation, realtime transcript, and audio endpoints under `https://followthrough.alhinai.dev/api/webhooks/omi/...` were saved and reopened; three toggles are on and the audio interval is five seconds.
- Local `ggml-tiny.bin` (74.1 MB) is ready. `RECORD_AUDIO` is running, the app is battery-whitelisted, and its foreground service remains active while the phone is Dozing.
- Physical-phone conversation finalization added 35 stored events. A fresh screen-off session kept local Whisper running and added 5 stored events after finalization.
- Current Omi limitation: with Basic cloud quota exhausted, on-device STT does not independently emit realtime transcript or raw-audio developer hooks. Conversation finalization works, so unattended delivery is eventual rather than realtime.
- Authenticated Omi is now an ambient authorization boundary: relevant speech may dispatch regardless of unreliable `is_user`, while speaker evidence is retained. A broad generic `to do` false positive was detected live, corrected to archive-only, removed from operational memory, and its nonrunning card cancelled.

## Recovery, credentials, UI, and soak

- Credential rotation: a temporary mode-0600 device token was accepted at the public edge (`202`), removed, and then rejected (`401`). The primary Omi token was not rotated.
- Backup `data/backups/phase7-20260711T224637Z` contains 11 mode-0600 files under a mode-0700 root. Manifest verification passed.
- Restore rehearsal into an explicit empty target passed; operations, archive, and effects databases each returned SQLite integrity `ok`. The rehearsal target was removed afterward.
- Authenticated Chromium visual check showed `Spark + Hermes online`, `Autonomy is running`, live archive/audio/job metrics, and all three emergency controls. No application JavaScript exception occurred; the strict CSP intentionally blocked Cloudflare's injected analytics beacon.
- One-shot soak passed all hard checks with a three-record, hash-chained, mode-0600 receipt.
- Prior bounded monitor checkpoints reported zero hard failures. Extended soak monitoring is optional.

## Credit routing applied after measurement

- Hermes last-day insight snapshot: 89.2M recorded tokens; cron accounted for 48.9M and subagents 29.8M.
- Main Hermes and the isolated Followthrough profile now use `gpt-5.6-luna` with low reasoning.
- Nine active LLM-driven routine cron jobs and default delegation are pinned to `google/gemini-3.5-flash`.
- Main fallback goes to Gemini Flash, then NVIDIA GLM, without a second Codex attempt.
- Deterministic archive, relevance, sandbox, controls, effect journal, backup, and soak paths make no model call.
