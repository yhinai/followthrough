# Phase 0 and 1 verification evidence — 2026-07-11

## Deterministic gate

- `pytest -q`: 11 passed.
- `ruff check .`: passed.
- `PYTHONPATH=. .venv/bin/python scripts/eval.py`: 3/3 passed.
- AES-256-GCM round-trip and authenticated-tamper rejection passed.
- Native audio gap detection reported sequence 1 missing from `[0, 2]`.

## Real Cloudflare edge gate

- Endpoint: `https://omi.alhinai.dev`.
- Unauthenticated `/api/runs`: HTTP 401, `Cache-Control: no-store`, CSP present.
- Authenticated native transcript: HTTP 202 in 39 ms.
- Identical transcript retry: HTTP 202 with `created=false` and the same run.
- Stock Omi transcript shape: HTTP 202; explicit non-owner segment was archive-only.
- Stock Omi PCM16 upload: HTTP 202; identical retry returned `created=false`; key reuse with different bytes returned HTTP 409.
- Encrypted PCM file mode: 0600; plaintext byte pattern absent.
- Background Hermes run completed in 6.1 seconds, with transcript absent from worker command arguments.

## Privacy and migration gate

- Operational `runs.text` plaintext rows: 0.
- Plaintext evaluation inputs: 0.
- Raw `signal` keys in step output: 0.
- Reports containing the legacy `**Signal:**` field: 0.
- Legacy reports were replaced only after their source transcripts were present in the encrypted archive.
- Dashboard, device-token directory, dashboard token, device token, archive key, encrypted audio, and generated reports have owner-only permissions.

## Runtime

- `followthrough.service`: active after restart.
- `/healthz`: database, archive, authentication, and Hermes CLI readiness true.
- Cloudflare public routing passed real external requests.
- Hermes Discord channel and gateway were healthy during the live runtime audit.

The remaining systemd exposure score is 8.5 because this is still a user service. Unknown repository code is not executed by this process; Phase 4 introduces the dedicated constrained runner boundary.
