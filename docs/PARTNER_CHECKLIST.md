# Partner power-up checklist

Each item is worth 25 points only after a mentor sees real, load-bearing use.

- [x] Cloudflare: public Followthrough URL is live through the existing tunnel. Show the live URL and Cloudflare dashboard.
- [ ] Wispr Flow: claim the event promotion, dictate at least 500 genuine event-day words, and retain the stats screenshot.
- [ ] Linkup: create/copy the event API key into the ignored secrets file; run a company signal and show the cited live query in its trace.
- [ ] ElevenLabs: redeem the Creator perk with organizer support, add the API key and voice ID, then play a completion brief live.
- [ ] Convex: sign in, create the Followthrough deployment, add `CONVEX_URL` and `CONVEX_DEPLOY_KEY`, and make the dashboard read authoritative run state from Convex.
- [ ] Dodo Payments: finish merchant onboarding, create the $9 Event Pass, configure live API/product/webhook values, and show a real checkout. A test transaction or account alone does not count.

Hermes/OpenAI is the eligibility runtime, not one of the six +25 power-ups. The existing OpenAI Codex OAuth session is healthy; no direct OpenAI key is required for Followthrough's Hermes manager.
